package jp.co.navitime.j07.p08;

/*
 * 以下の要件に従い, CSV データを読み込み,
 * 1 列目を昇順にソートした CSV データを出力せよ.
 * なお本問において, CSV は「各要素はコンマを含まない」「各要素はクオートされていない」ことを前提としてよい.
 *
 * 1. Main の引数として 入力ファイルのパス, 出力ファイルのパスを指定できる.
 * 2. CSV 入力部, ソート部, CSV 出力部には単体テストが実装されている.
 *      - それ以外への実装は任意とする.
 * 3. 単体テストは外部ファイルを読み込まずに実行できる.
 */
public class Main {

    public static void main(String[] args) {
    }
}
