package jp.co.navitime.j02.p07;

/*
 * 1. 下記のような Food クラスを作成せよ。
 *
 * a. 下記フィールドを持つ
 *
 *   - name (食品名) : String
 *   - stock (在庫数) : int
 *
 * b. コンストラクタとして Food(String name, int stock) を持つ
 * c. 情報表示メソッド printStock() は「xxxの在庫はooo個です」と表示する
 * d. 発注メソッド order(int numOrders) は在庫数を numOrders 個増やし「xxxをooo個注文しました」と表示する
 *
 * 2. Food クラスを使用して下記のように表示するプログラムを作成せよ。
 *
 *   トマトの在庫は7個です
 *   トマトを15個注文しました
 *   トマトの在庫は22個です
 */

public class Main {

    public static void main(String[] args) {
    }
}
